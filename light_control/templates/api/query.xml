<?xml version="1.0" encoding="utf-8"?>
<lights>
    {% for node in nodes %}
    <light node="{{node.nodeId}}">
        <nodeId>{{node.nodeName}}</nodeId>
        <lastActive>{{ node.lastActive|date:"U" }}</lastActive>
        {% for light in node.light_set %}
            <red{{light.controller}}>{{light.color|slice:"0"}}</red{{light.controller}}>
            <green{{light.controller}}>{{light.color|slice:"1"}}</green{{light.controller}}>
            <blue{{light.controller}}>{{light.color|slice:"2"}}</blue{{light.controller}}>
        {% endfor %}
    </light>
    {% endfor %}
</lights>
